# 手動驗收清單（尚未執行）

請先完成 README 設定，在測試 Channel 與測試 D1 執行。以下是供你操作的驗收步驟；不會自動建立活動或執行測試。

## 知識與前端

- 開啟本機 Pages 頁面，點四個常用問題，核對答案與資料來源；手機寬度下確認表單與活動卡片可閱讀。
- 在 LINE 傳送「隊規」，核對新成員、品牌對外活動、管理事項三條規定完全符合已確認內容；「品牌資料」包含完整品牌語與標語。
- 傳送未知問題，收到「目前沒有已確認資料」；管理員私訊 `/admin pending` 可看到該問題。
- 問「管理事項是不是不用三票」，應列為待補問題，不猜測或回覆相反規定。
- 網頁查詢未知問題不新增 D1 記錄；停止本機 Worker 後，前端呈現連線錯誤，不能假裝知識庫查無資料。
- 未建立活動時顯示沒有已確認資料，不出現虛構日期與地點。

## LINE 與授權

- Console Verify 成功，私訊及群組的知識查詢均可回覆。
- 以一般 HTTP 客戶端送未簽章 `POST /webhook`，應回 401 且 D1 無任何異動。
- 若自行準備本機簽章請求：以原始 UTF-8 bytes 簽章，先確認成功；保留原簽章卻變更空白／body 內容時應回 401。不要把 Secret 放入共享檔案或日誌。
- 有效簽章 `{"events":[]}` 回 200；有效簽章但壞 JSON／缺 events 回 400。
- 圖片、貼圖、空白文字或缺 replyToken／webhookEventId 的事件應忽略；同一批的其餘有效事件仍能處理。
- 未設定管理員白名單時管理命令拒絕；非管理員私訊拒絕；白名單管理員在群組操作也拒絕；白名單管理員私訊才可操作。
- `POST /api/knowledge`、`POST /api/activities` 均不得寫入；瀏覽器下載的 HTML、JS 不應有任何 Channel Secret、Token 或管理員 ID。

## 報名與重送

- 沒有開放活動時「我要報名」明確告知不可報名；新增一場未來活動後直接要求姓名。
- 新增第二場活動後，先要求「選擇 活動ID」，選擇不在清單的 ID 不得進入姓名步驟。
- 在測試資料中建立名額 1 的活動。帳號 A 報名為正取；帳號 B 報名為候補。以 D1 Console 核對 `registrations`。
- A 再次報同一場活動，仍只有一筆記錄；改由另一群組報同一場也不可重複。
- 兩位帳號接近同時報最後一席，正取數不能超過 capacity。
- 在姓名步驟前關閉活動或讓開始時間經過，提交姓名不得新增報名。
- 同一人在不同群組及私訊各自開啟對話，確認狀態隔離；等待 15 分鐘後姓名步驟提示重啟。
- 原事件 ID 重送時，pending_questions／registrations／admin_audit 均不重複新增。重送應保存相同事件內容，不可把事件 ID 任意套到另一事件。
- 測試環境可暫時使用無效 Access Token，使 Reply API 失敗：事件資料只能寫一次。修復 Token 後在 token 有效期間重送，應讀取原回覆；即使回覆 token 已失效，「我的報名」仍應反映已提交資料。
- 缺少 source.userId 的群組事件不能報名，應提示私訊；不可把群組 ID 當成個人身分。

## 正式發布前

- 確認正式資料只有已核准內容，移除測試活動與測試個資；採用獨立 D1 更容易隔離。
- 確認 Pages 僅發布 `public/`、CORS origin 正確、群組與 Webhook 開關啟用、Worker Secrets 已設定。
- 確認 Cloudflare 管理者權限、資料保存政策及候補人工處理流程。
